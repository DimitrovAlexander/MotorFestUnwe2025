<!-- Calendar.cshtml -->
@* @model IEnumerable<MotorFest.Models.Event.EventViewModel>

<h1>Календар на събитията, за които сте се записали</h1>

<div class="calendar-container">
	@foreach (var eventGroup in Model.GroupBy(e => e.EventDate.Date))
	{
		<div class="day-events">
			<h3>@eventGroup.Key.ToString("dd MMM yyyy")</h3>
			<ul>
				@foreach (var ev in eventGroup)
				{
					<li>
						@if(ev.IsCanceled){
							ev.Name = $"{ev.Name} - ОТМЕНЕНО";
						}
						@if (ev.EntranceFee == 0)
						{

							<a asp-action="Details" asp-route-id="@ev.Id" class="event-link">
								@ev.Name - Вход свободен
							</a>
						}
						else
						{
							<a asp-action="Details" asp-route-id="@ev.Id" class="event-link">
								@ev.Name - Вход: @ev.EntranceFee лв.
							</a>
						}
					</li>
				}
			</ul>
		</div>
	}
</div> *@
  @model IEnumerable<MotorFest.Models.Event.EventViewModel>

<div class="container mt-5">
	<h2 class="mb-4 text-center">Моите запазени събитията</h2>
	<div class="text-end">
		<a asp-action="ExportCsv" class="btn custom-primary-btn mt-3 mb-3">Export CSV</a>
	</div>


	@foreach (var eventGroup in Model.GroupBy(e => e.EventDate.Date))
	{
		<div class="card mb-4 shadow">
			<div class="card-header ">
				<h4 class="mb-0">@eventGroup.Key.ToString("dd MMM yyyy")</h4>
			</div>
			<div class="card-body">
				<div class="row">
					@foreach (var ev in eventGroup)
					{
						<div class="col-md-6 col-lg-4 mb-3">
							<div class="card event-card">
								<img src="@ev.EventLogo" class="card-img-top img-fluid rounded-top" alt="Снимка на събитието">
								<div class="card-body">
									@if (ev.IsCanceled)
									{
										<h5 class="card-title">@ev.Name - ОТМЕНЕНО</h5>

									}
									else{

									<h5 class="card-title">@ev.Name</h5>
									}

									<p class="card-text">
										@if (ev.EntranceFee == 0)
										{
											<span class="text-success">Вход свободен</span>
										}
										else
										{
											<span class="text-danger">Вход: @ev.EntranceFee лв.</span>
										}
									</p>
									<a asp-action="Details" asp-route-id="@ev.Id" class="btn btn-primary w-100">Детайли</a>
								</div>
							</div>
						</div>
					}
				</div>
			</div>
		</div>
	}

	<div class="text-center">
		<a asp-action="Index" class="btn custom-primary-btn mt-3">Назад</a>
	</div>
</div>

